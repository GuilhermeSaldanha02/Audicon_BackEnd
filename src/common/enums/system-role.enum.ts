/**
 * Papel do usuário no sistema, por empresa (modelo do SDD v3.0).
 *
 * Único modelo de papel após R-03+04 (substituiu o legado por condomínio).
 */
export enum SystemRole {
  MASTER = 'MASTER',
  GERENTE = 'GERENTE',
  FUNCIONARIO = 'FUNCIONARIO',
}
