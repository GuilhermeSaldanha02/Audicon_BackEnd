/**
 * Gravidade da infração, classificada pelo operador no momento do registro.
 * Conceito central do produto (badges, filtros, contexto para a IA).
 */
export enum InfractionSeverity {
  LEVE = 'LEVE',
  MEDIA = 'MEDIA',
  GRAVE = 'GRAVE',
}
